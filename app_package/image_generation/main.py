import os
from app_package.helpers.directory_helper import IMAGE_GENERATION_DIR

def link_to_reels_action_image_generation(basename, emotion_analysis, channel_niche, logging_object):
    #Output filepath
    image_generation_file_output = os.path.join(IMAGE_GENERATION_DIR, f"{basename}.png")

    # if not emotion_analysis:
        #Generate emotion and segment analysis 
    
    # Create another model that creates a story based on emotino and segment and then channel_niche and a segment text if available and lastly a segment text

    # Generate the image

    # Return the emotion analysis (contains emotion and segment.)


    